/**
 * Заполняет базу начальными данными.
 * Запускать один раз: node seed.js
 * При повторном запуске данные не дублируются (INSERT OR IGNORE / INSERT OR REPLACE).
 */
import 'dotenv/config';
import { setProducts, setConfig } from './services/db.js';

// ─── Список услуг ────────────────────────────────────────────────────────────
// Редактируйте этот массив и перезапускайте seed.js чтобы обновить список в боте
const PRODUCTS = [
  'Разработка сайта',
  'SEO-продвижение',
  'Контекстная реклама',
  'SMM',
  'CRM-интеграция',
  'Другое',
];

// ─── Настройки бота ──────────────────────────────────────────────────────────
const COMPANY_NAME = 'ВашаКомпания';

const FAQ_PROMPT = `Ты помощник компании "${COMPANY_NAME}". 
Кратко и по делу отвечаешь на вопросы клиентов о наших услугах: разработка сайтов, SEO, контекстная реклама, SMM, CRM-интеграция.
Если вопрос не по теме — вежливо предложи записаться на встречу с менеджером.
Отвечай на русском языке.`;

// ─── Запись ──────────────────────────────────────────────────────────────────
setProducts(PRODUCTS);
setConfig('company_name', COMPANY_NAME);
setConfig('faq_prompt', FAQ_PROMPT);

console.log('✅ База данных заполнена:');
console.log(`   Услуг: ${PRODUCTS.length}`);
console.log(`   Компания: ${COMPANY_NAME}`);
console.log('   FAQ-промпт сохранён.');
