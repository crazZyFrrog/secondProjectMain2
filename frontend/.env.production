# Локальная проверка «как на Vercel»: скопируйте этот файл как .env.production
# (файл .env.production в git не попадает), затем: npm run verify:prod
#
# cp .env.production.example .env.production   (macOS/Linux)
# copy .env.production.example .env.production (Windows CMD)

VITE_API_URL=https://conslanback-vladislav7599.amvera.io/api
