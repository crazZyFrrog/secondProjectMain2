meta:
  environment: docker
  toolchain:
    name: docker
build:
  dockerfile: Dockerfile
run:
  persistenceMount: /data
  # Порт процесса в контейнере и порт сервиса должны совпадать с тем, на чём слушает uvicorn (см. Dockerfile).
  containerPort: 80
  servicePort: 80
